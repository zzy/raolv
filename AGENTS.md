## Git 提交信息规范

### 语言
中文优先、英文辅助

### 格式
<类型>: <简洁>

## 编码规范

### 规则
- raolv 为底座、mousuo 为试验场：共享层（非业务）文件改动必须两项目同步，并以 diff 验证逐字一致（业务差异除外）
- 模块组织用侧边文件聚合（`dir/` 子模块由同层 `dir.rs` 声明），禁用 `mod.rs`
- 遵循 Topcoat（源码在 `../tmp/topcoat`）规范和最佳实践
- 变量、文件、模块等命名须严格语义化，严谨随意敷衍
- 严禁死代码，及重复定义，最高代码复用度
- 禁止冗余克隆，能避免 format! 则避免
- 遵循 SurrealDB 规范和实践，严禁硬编码
- 代码注释中文优先，英文辅助
- 严禁任何绝对路径
